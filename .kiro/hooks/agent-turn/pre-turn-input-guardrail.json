{
  "name": "pre-turn-input-guardrail",
  "description": "Run input-sanity guardrail (size, encoding, schema, injection markers, PII scan) before any agent turn proceeds. Refuse-and-halt on failure.",
  "category": "agent-turn",
  "trigger": {
    "event": "agent.turn.pre"
  },
  "actions": [
    {
      "run": "tools/guardrail/input-check.ts --agent=${agent} --turn=${turn_id}",
      "fail_on": "guardrail_block"
    }
  ],
  "governance": {
    "pii_class": "guarded",
    "halts_turn_on_block": true,
    "agent_visible": "all"
  }
}
